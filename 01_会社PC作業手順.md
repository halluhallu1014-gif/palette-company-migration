# 会社PC作業手順

## 1. ブックを作る

1. Excelで空のブックを作ります。
2. 「Excelマクロ有効ブック（*.xlsm）」で保存します。
3. `Alt + F11`でVBAエディタを開きます。

## 2. 標準モジュールを作る

「挿入」→「標準モジュール」を6回実行し、プロパティの`(オブジェクト名)`を次に変更します。

1. `modUIStyle`
2. `modExMain`
3. `modExPaste`
4. `modPalette`
5. `modCompanySetup`
6. `modCompanyCheck`

## 3. UserFormを作る

「挿入」→「ユーザーフォーム」を3回実行し、Nameを次に変更します。

1. `frmPalette`
2. `frmExCollect`
3. `frmExPaste`

コントロールは[03_フォーム作成手順.md](03_フォーム作成手順.md)に従います。位置や色は適当で構いません。種類とNameだけは正確にしてください。

## 4. コードを貼る

README記載の順番で、GitHubの各コードファイルを開きます。「Raw」を押し、全選択してコピーし、対応するモジュールまたはフォームコードへ貼り付けます。

既に自動挿入された空のイベントコードがある場合は、フォームコード欄を全選択してから置き換えてください。

## 5. セットアップする

1. VBAエディタで「デバッグ」→「VBAProjectのコンパイル」を実行します。
2. 「変数が定義されていません」と表示された場合は、青く選択されたコントロールのNameを修正します。
3. Excelへ戻ります。
4. `Alt + F8`を押します。
5. `Palette_Company_Setup`を実行します。
6. 完了メッセージを確認します。

## 6. 診断する

1. `Alt + F8`から`Palette_Company_Check`を実行します。
2. `zz_PaletteCheck`シートを開きます。
3. 「NG」があれば、対象のNameまたは見出しを修正します。
4. セットアップと診断は何度実行しても構いません。

## 7. 起動する

`Alt + F8`から`Palette_Open`を実行します。
