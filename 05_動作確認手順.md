# 動作確認手順

## セットアップ確認

1. `Palette_Company_Setup`が完了する。
2. `zz_ExCategory`、`zz_ExData`、`zz_ExSetting`が作成される。
3. 再実行しても登録済みデータが消えない。
4. `Palette_Company_Check`の結果にNGがない。

## Palette

1. `Palette_Open`でモデルレス表示される。
2. 「例文収集」と「例文貼付」だけが表示される。
3. 各「開く」から対応フォームが開く。
4. 閉じるが動く。

## 例文収集

1. Word選択文字列を取得できる。
2. Excel単一セルの表示文字列を取得できる。
3. 複数セルが同じ行は空白、行ごとは改行で連結される。
4. 結合セルが重複しない。
5. 手入力、本文訂正、例文名自動採番が動く。
6. 複数分類で同じ文書番号の複数行が登録される。
7. 登録後に分類と出典が残る。
8. 登録済み文書の読込と一括更新が動く。

## 例文貼付

1. 第1分類選択後、第2分類の先頭が自動選択される。
2. 操作を追加せず候補が表示される。
3. 前回使用分類が次回起動時に復元される。
4. 候補が使用回数、最終使用日、文書番号の順で並ぶ。
5. 5件単位のページ切替が動く。
6. 1回クリックで編集対象を選択できる。
7. ダブルクリックでWordまたはExcelへ貼り付けられる。
8. 使用回数と最終使用日が更新される。
9. 編集から収集フォームが訂正モードで開く。
